name: omniroute
version: 0.2.0
description: "Omniroute backends Hermes: image generation (/v1/images/generations), web search (/v1/search), and text-to-speech (/v1/audio/speech) via OpenAI-compatible Omniroute router."
author: Jose Victor
kind: backend
provides_web_providers:
  - omniroute
requires_env:
  - OMNIROUTE_TOKEN
